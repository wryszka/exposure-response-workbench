# Build decisions log — Exposure & Event Response Workbench

> Dated, reverse-chronological, self-contained entries. The single source of truth for what changed and why.

## Accepted at kickoff (2026-09-17)
- **D1 — Origin & purpose.** Built from the Hiscox Europe exposure-management call (2026-09-16): automated wildfire/cat exposure tracking — overlay live hazard footprints on insured property locations, show threatened properties now, alert stakeholders. Anchor peril wildfire; broadened to flood + windstorm at the user's direction. Review session targeted **Fri 16 Oct 2026**.
- **D2 — Naming.** Repo `wryszka/exposure-response-workbench`; schema `exposure_response`; tile "Exposure & Event Response" (promotes the existing `exposure-management` roadmap tile in the actuarial-workbench hub → live).
- **D3 — All three perils, one spine.** Wildfire + flood + windstorm built on ONE event-response spine (footprint model, intersection engine, exposure view, alert path) + three thin adapters (footprint source differs). Wildfire first and deepest.
- **D4 — Hazard data = live + frozen + Marketplace.** Use free live feeds where they exist (FIRMS/EFFIS fire; MeteoAlarm/NHC/IBTrACS wind; GloFAS/EFAS + Copernicus EMS flood), a frozen snapshot for a reproducible room, synthetic fill where live data is thin (esp. flood live-event layer), and check Databricks Marketplace geo/property listings (CARTO/Precisely/MBI) — verify in-workspace before relying on them. Property book stays synthetic (no PII).
- **D5 — Working alert.** A real Databricks job detects newly-threatened properties over threshold → stakeholder digest to a demo inbox, **grouped by event, not by country** (directly fixes Hiscox's siloed cross-border miss). (P4.)
- **D6 — Entity/geography.** Bricksurance SE, European property book (FR/IT/AT/ES/DE), lat/long geocoded — the cross-border story needs real coordinates, not just UK postcodes.

## P0 (2026-09-17) — scaffold + property foundation — newest first
- **D7 — Geospatial pattern confirmed.** `ST_` functions are live on warehouse `a3b61648ea4809e3` (Serverless Starter). They require **GEOMETRY** (not GEOGRAPHY) and a serverless/DBR-17.1+ warehouse. Verified compute path for the 50/100/200 m bands: `ST_DWithin(ST_Transform(ST_GeomFromText(wkt,4326),3035), ..., metres)` — EPSG:3035 (LAEA Europe) gives true metres (two Paris points 0.0008° apart → 58.7 m; within_100m=true, within_50m=false). **Biggest technical risk retired in P0.**
- **D8 — Data model.** `3_property` (~5,000 synthetic insured objects, columns mirror data-core `insured_object` + property extensions) with a dense **Var wildfire cluster** (SE France) and a **cross-border alpine cluster** (N Italy ↔ Austria). `2_event_footprint` holds frozen WKT footprints for the three perils; the Alpine Flood event has two segments under one `event_id` (the cross-border seed). Live feeds land in the same `2_event_footprint` shape in P2.
- **D9 — Auth/profile.** Deploy on the **DEV** profile (`-p DEV`, host fevm-lr-dev-aws-us). The DEFAULT/serverless-workspace profile token is expired — not used here.

## P1 (2026-09-17) — governed exposure functions + live exposure view — newest first
- **D14 — App live + verified.** App `exposure-response-workbench` deployed to dev, RUNNING. URL `https://exposure-response-workbench-7474656169654171.aws.databricksapps.com`. Verified over HTTP (auth token): `/api/config`, `/api/events` (59/122/260 threatened), `/api/event/EVT_FIRE_VAR?buffer=200` (59 props, bands 50/1/8, €164.3m), `/api/event/EVT_FLOOD_ALPS` (cross-border IT 69 / AT 53), root SPA 200 with `Cache-Control: no-store`.
- **D13 — App SP grants.** App SP `fc3ac835-8393-4ea6-8d2b-180096836d7b` (client id) granted USE CATALOG/SCHEMA, SELECT on `3_property` + `2_event_footprint` (UC SQL functions run **invoker's rights** → the SP needs table SELECT, not just EXECUTE), and EXECUTE on the three functions. Warehouse CAN_USE comes from the `resources/app.yml` binding. SP recorded in `databricks.yml` dev target.
- **D12 — `ST_*` is warehouse-only; notebooks can't create these functions.** `spark.sql('CREATE FUNCTION … ST_…')` fails on serverless notebook (Spark Connect) with `SparkConnectUdfRejectionScope`. Fix: notebook `02_exposure_functions.py` orchestrates the DDL onto the **SQL warehouse** via the Statement Execution API (`WorkspaceClient().statement_execution`). Portable (catalog/schema/warehouse from widgets), re-runnable, and the job (`exposure_02_functions`) runs green.
- **D11 — Governed functions (the compute path).** `fn_exposure_in_footprint(p_event_id, p_buffer_m)` (threatened objects + true metres + band; multi-segment events unioned via `ST_Union_Agg`), `fn_event_exposure_summary(…)` (rollup by band and by country), `fn_events(p_buffer_m DEFAULT 200)` (picker with live threatened-count). Rich `COMMENT` on each so agents/Genie can route later. All math lives here, never in the app.
- **D10 — Map is CSP-safe inline SVG.** No external tile/CDN (strict self-contained HTML). The app returns footprint WKT + property lat/long; the browser parses the `POLYGON((…))`, projects lon/lat into a viewBox (cos-latitude x-correction) and draws the footprint polygon + property points coloured by band. Reused because the sibling reinsurance app renders geo as tables, not a plotted map.

## P2 (2026-09-17) — live hazard-feed ingestion — newest first
- **D20 — MeteoAlarm is the live proof, and it runs on serverless.** `12_feed_windstorm` pulls current warnings from `feeds.meteoalarm.org/api/v1/warnings/feeds-{country}` (keyless CAP-JSON, 38 EU met services) → the feeds job ran green on serverless (egress confirmed). Verified end-to-end: pulled 15 live FR warnings, landed event `EVT_LIVE_MA_FR_STORM_20260917`, **195 threatened Bricksurance-SE properties / €498.6 m** through the same `fn_exposure_in_footprint` (192 in the 0–50 m band). Sits alongside the frozen events with `is_live=true`, sorted first in the picker.
- **D21 — Region → polygon via GISCO NUTS3 (2013 vintage).** MeteoAlarm France warns by **NUTS3** code; other countries by **EMMA_ID**. FR codes (FR421, FR823 Alpes-Maritimes, …) are the **NUTS 2013** vintage — 2016/2021 renumbered them (FR823→FRL03), so we join Eurostat GISCO `NUTS_RG_20M_2013` boundaries (20M generalisation = compact WKT, fine for region-scale warnings). Cached to Volume `reference`. EMMA_ID→boundary is the documented next step; those pulls land to bronze meanwhile.
- **D22 — Live + frozen + credential-gated, one shape.** Live events land in `2_event_footprint` (added cols `is_live`, `source_detail`, `ingested_at`; backfilled), tagged `source ∈ {METEOALARM, FIRMS, EMS, GLOFAS, EFFIS, SYNTHETIC}`. Adapters never crash: FIRMS needs a free MAP_KEY and GloFAS/EFAS a free CDS key (secret scope `exposure_response`) — absent, they fall back to **frozen real-shape samples** (`is_live=false`). Footprints stored as **WKT text** → adapters need no `ST_` (the functions do the geospatial maths on the warehouse). Every raw pull audited to `1_hazard_feed_raw` (bronze).
- **D23 — `fn_events` now carries source + is_live.** Recreated to return `source` and `is_live` (and sort live-first) so the app badges live vs frozen; re-granted EXECUTE (the CREATE-OR-REPLACE gotcha). SVG map parser upgraded to handle **MULTIPOLYGON** (live region footprints) as well as POLYGON. App redeployed + HTTP-verified: `/api/events` shows all 6 events with flags; the live event returns 195 properties + 19 MULTIPOLYGON segments.

## P4 (2026-09-17) — stakeholder alerts — newest first
- **D30 — Alert grouped by EVENT, never by country (the whole point).** The dispatch is one row per event per sweep; the digest carries every country the event touches in a single alert. Verified: both flood events (`EVT_FLOOD_ALPS`, the Copernicus-EMS frozen sample) dispatch as one alert each with `n_countries=2`, IT+AT together — the exact cross-border miss the Hiscox team described, fixed structurally. The email also carries an explicit "Cross-border event — country-siloed teams would each see only their slice" banner.
- **D29 — Snapshot + delta = new / progressing / extinguished.** `gov_exposure_snapshot` (append-only, one row per threatened property per event per run) is the baseline; `fn_event_delta(p_event_id, p_buffer_m)` compares the CURRENT live exposure against the most recent snapshot → `newly_threatened` / `still_threatened` / `no_longer_threatened`. First sweep: everything is newly_threatened (no prior snapshot). Verified non-vacuous: after the baseline, `fn_event_delta('EVT_FLOOD_ALPS',200)` → 122 still / 0 new / 0 gone; a growing footprint surfaces new properties. Snapshot is written AFTER the delta is computed (so it becomes the next run's baseline) and uses the warehouse (`ST_` path).
- **D28 — Per-peril threshold, visible & tunable.** `ref_alert_threshold` (peril → `min_gross_eur`, `min_new_count`): an event alerts when modelled gross loss ≥ the floor OR newly-threatened count ≥ the count. Seed: FIRE €10 m/15, FLOOD €15 m/20, STORM €25 m/40. Kept as a table so the trigger is auditable, not buried in code. All 6 current events breach → status SENT (channel configured) or RENDERED_NOT_SENT (no channel).
- **D27 — Pluggable real sender; never blocks on a credential.** `send_digest` tries SMTP (secrets `exposure_response/SMTP_HOST|PORT|USER|PASS|FROM|TO`), then a Slack webhook (`exposure_response/SLACK_WEBHOOK`), else returns "no channel". Status recorded per event: **SENT** (breached + sent), **RENDERED_NOT_SENT** (breached, no channel secret — the in-app digest preview is the proof), **SUPPRESSED** (below threshold). Current state: RENDERED_NOT_SENT for all (no secret set) — see `docs/ALERTS.md` to add one and it sends for real, no code change. Serverless egress for SMTP/webhook already proven in P2.
- **D26 — Audit everything (`gov_alert_dispatch`, append-only).** One row per event per sweep: threshold decision + rule, the governed numbers (threatened count/SI, gross/ceded/net), the cross-border country list, the delta counts, channel + recipients, status, and the exact `digest_html`/`digest_text` sent. Written with a Spark append (pure data — no `ST_`, so no warehouse round-trip needed for the write); the snapshot INSERTs and `fn_event_delta` use the warehouse.
- **D25 — Digest built in Python, stored, previewed in-app.** The email (inline-styled HTML + plain text) is composed in `30_alerts.py` from the governed function outputs and stored on the dispatch row. The app renders the stored `digest_html` in an isolated `srcdoc` iframe (the "Preview the alert that goes out" button) — so the preview IS the real dispatched artefact, not a mock. No CSP meta blocks it (the SPA already runs inline scripts/styles).
- **D24 — Runs with nobody logged in + a manual trigger.** Job `exposure_30_alerts` (serverless, hourly schedule, PAUSED by default → armed for the demo) is the "no human" proof; ran green (TERMINATED SUCCESS, ~1m40s). The app's "Run alert sweep now" button POSTs `/api/alerts/run` → `jobs.run_now` (app SP granted **CAN_MANAGE_RUN** on the job; degrades gracefully if not permitted). App redeployed (deployment `01f1b2bd51fd1bf3bb3058c7924be5e2`) + HTTP-verified: `/api/alerts` lists the 6-event log, `/api/event/EVT_FLOOD_ALPS` returns the alert (RENDERED_NOT_SENT, IT+AT, 5,078-char digest) + delta (122 still).

## P7 (2026-09-18) — Event Radar: the unstructured news sensor ("we knew before the satellite") — newest first
- **D41 — Verified over HTTP + live agent turn.** App redeployed (deployment `01f1b34dcc43114d8818a14a3951b5f5`, "App started successfully", RUNNING). `/api/radar` returns the **13 signals** (1 frozen hero + 12 live GDACS), hero `beats_feed=true`; the SPA serves the **Event Radar** tab (radarScreen, ⚡"beats the feed" badge, "Promote to event & alert"). A **live** agent turn ("what's on the Event Radar that beats the feed?") called the new `news_radar` tool and narrated the governed numbers (36 props / €103.6m / confidence 1.0) — no invented figures.
- **D40 — The hero, proven end-to-end.** Frozen synthetic press item (fictional wire, no real names) → `ai_classify`=**FIRE / HIGH**, geocoded to **Var**, published **2026-09-15 10:00** → `fn_news_radar()`: **36 insured properties within 3 km / €103.6m**, `corroborated=false`, **`beats_feed=TRUE`**, confidence **1.0**, status **verified**. FIRMS sample event (`EVT_FIRMS_SAMPLE_FR`) is dated **2026-09-16** → the **news leads the satellite feed by 14 hours**. Human-gated promote (simulated as the app POST) inserted `EVT_NEWS_NEWS_FROZEN_VAR_FIRE` (`source='NEWS'`, `is_live=true`) → the **existing** `fn_exposure_in_footprint(…,200)` returned **30 props / €90.6m**, and the decision was audited to `gov_news_decision` (action PROMOTE, conf 1.0). Then **reset** (event + audit row deleted) so the presenter clicks Promote live.
- **D39 — Auto-detect, HUMAN-decide (never autonomous).** The pipeline scans, extracts, geocodes, verifies and recommends; **promoting** a signal to an event and **alerting** is a human click (`POST /api/radar/promote`, idempotent), audited append-only in `gov_news_decision` (signal_id, action, event_id, decided_by, confidence, evidence). Nothing consequential runs on its own — the promoted event then flows through the *same* exposure view + alert path as any feed event.
- **D38 — Verification is the crux, disconfirmation-first (`fn_news_radar`).** For each signal: (a) buffer the geocoded point 3 km and intersect `3_property` on the `ST_`/EPSG:3035 path → threatened_count + sum_insured; (b) `corroborated` = a structured **FEED** event (`source ∈ FIRMS/EFFIS/METEOALARM/GLOFAS/EMS`, *not* SYNTHETIC/NEWS) of the same peril at/before the signal time within 40 km. **`beats_feed` = touches the book AND not yet corroborated.** Confidence + status derived (touches-book is the gate; a signal that doesn't touch the book scores 0.15/dismissed → no false-positive on "a fire in Var" that isn't near the book). Evidence is a plain-language trail. The app + agent read decisions only from here.
- **D37 — Extraction = real AI Functions + a governed gazetteer.** `2_news_signal` built on the warehouse: `ai_classify` for peril (FIRE/FLOOD/STORM/OTHER) + severity (LOW/MEDIUM/HIGH), `ai_query` (Claude `databricks-claude-sonnet-4-6`) for a one-line summary; location comes from `ref_gazetteer` (17 demo places, longest-name substring match) — deterministic geocode, so the threatened numbers are stable while the extraction is genuinely AI.
- **D36 — Ingestion: live GDACS RSS + a frozen synthetic press item.** `notebooks/40_news_radar.py` reuses the P2 robust-fetch pattern to pull **GDACS RSS** (`gdacs.org/xml/rss.xml`, keyless, real) — landed **12 live** current disaster items (wildfire/flood/cyclone) to bronze `1_news_raw` (`is_live=true`). The frozen wire item (dated `EVT_FIRMS_SAMPLE_FR.event_date − 14h`, so the gap survives a reset) makes "news beats the feed" deterministic for the room. Job `exposure_40_news_radar` (serverless, PAUSED) runs green.

---

## Shared gotchas (inherited across the demo family — verified)
- **`CREATE OR REPLACE FUNCTION` revokes EXECUTE grants** → re-grant the app SP after any function recreate, or the app 500s.
- **Genie space creation:** programmatically via the Databricks API (`databricks api post /api/2.0/genie/spaces --json @file`), never hand-built; grant app SP `CAN_RUN`; keep ≤ ~30 tables.
- **Apps serve stale HTML:** add middleware forcing `Cache-Control: no-store` on `text/html`.
- **Apps config:** `app.yaml` uses `valueFrom` (camelCase); Apps listen on port 8000; app SP needs schema USE/SELECT/EXECUTE, warehouse CAN_USE, Genie CAN_RUN, reset-job CAN_MANAGE_RUN.
- **Bundle auth on macOS:** `databricks bundle deploy` may need an unsandboxed shell (keychain) or it exits 45.
- **Serverless + files:** `%pip` installs; write to `/tmp` then copy; explicit schema on pandas→Delta appends; no `cache()`.
- **Geospatial:** GEOMETRY not GEOGRAPHY; reproject to a metric CRS (EPSG:3035 for Europe) before distance; SRID mismatch silently returns wrong distances.
- **Determinism:** seed the generator (42), math driver-side, sort deterministically; heroes deterministic conditional on the as-of date, which rolls to today on reset.
- **SQL table-function params don't resolve inside a correlated `LATERAL` subquery** (P7): `fn_news_radar(p_buffer_m …)` failed `UNRESOLVED_COLUMN p_buffer_m` when the param was referenced inside a `LEFT JOIN LATERAL (…)`. Outer-table correlation (`s.longitude`) works; the *function parameter* does not. Fix: make it a literal (the buffer is fixed 3 km) or hoist it into a CTE.
- **AI-function statements exceed the 50s Statement-Execution `wait_timeout`** (P7): `ai_classify`/`ai_query` over many rows return state `PENDING`, not `SUCCEEDED`, so a naive `run_wh` treats it as failure. Fix: **poll `get_statement`** until terminal (up to a few minutes).
- **`CREATE OR REPLACE TABLE` drops table grants too** (P7) — not just functions. Re-grant SELECT to the app SP + `account users` after rebuilding a table (e.g. `2_news_signal`), same as the function-recreate gotcha.
- **Large RSS/news feeds must be bounded** (P7): GDACS RSS carries ~340 items; a plain idempotent MERGE accumulates them across runs. Use **delete-then-append** of a capped set so the live table stays demo-sized.

## P3 (2026-09-17) — gross → net waterfall + coverholder split — newest first
- **D27 — App verified over HTTP.** App redeployed (deployment `01f1b2bb601718a8ae855feef1a44645`, "App started successfully"). `/api/event/{eid}` now returns `waterfall` + `coverholders` alongside the existing keys; confirmed with an auth token for `EVT_FLOOD_ALPS` (6 waterfall rows gross €147.5m → net €25.0m; 3 coverholders led by Alpine Cover Underwriting 78 props/€214.7m). Two new cards in the exposure view: a gross→net step waterfall (gross/ceded/net headline + per-layer bars) and a by-coverholder bar list (delegated vs direct badges). Learn step 5 added.
- **D26 — Coverholder / delegated-authority split.** `3_coverholder` (5 entities: 4 delegated MGAs/binders + the direct book) and a deterministic `3_property_coverholder` assignment (by country + stable `xxhash64` split, reset-safe). `fn_exposure_by_coverholder(event, buffer)` splits threatened exposure by writer. Verified: Var wildfire → Midi Méditerranée MGA 34/€85.3m + direct 25/€79.0m; Alpine flood → Alpine Cover Underwriting (delegated) 78/€214.7m + DACH binder 28/€71.7m + direct 16/€45.1m. This is the delegated-authority view Hiscox asked for.
- **D25 — Modelled loss via a visible governed table, not a hidden number.** Sum-insured-at-risk becomes a **modelled gross loss** through `ref_damage_factor` (peril × band damage ratio: e.g. FIRE 0.60/0.25/0.08, FLOOD 0.45/0.20/0.06, STORM 0.18/0.08/0.03). Kept as a governed table (not app logic) so the assumption is auditable and tunable — a cat model would replace it. Honest framing: cat treaties respond to *losses*, so we model the loss first, then cede it — this also seeds the later "predict cost impact" ask.
- **D24 — Property Cat XL programme + the waterfall function.** `3_treaty` (Bricksurance SE Property Cat XL 2026, EUR, covers FIRE/FLOOD/STORM, €25m retention) + `3_treaty_layer` (4 layers: €50m xs €25m, €75m xs €75m, €100m xs €150m @90%, €150m xs €250m @80% — top €400m). `fn_gross_to_net(event, buffer)` returns the waterfall: gross modelled loss → per-layer cession `LEAST(GREATEST(loss−attachment,0),limit)×placement` (mirrors the reinsurance workbench idiom) → net retained, with a running net after each layer. Verified all three events land at €25m net (each loss sits within the placed tower, so we retain the programme retention) off varying gross (€87.2m / €147.5m / €88.4m). Functions created on the SQL warehouse via the Statement Execution API (same `ST_` gotcha as notebook 02); job `exposure_20_treaty` runs green; EXECUTE + table SELECT re-granted to `account users` + app SP.

## P5 (2026-09-17) — event-response agent + Genie + governance — newest first
- **D34 — Verified over HTTP (live app).** App redeployed (deployment `01f1b2bf7e221041b444701bc54fbc2d`, "App started successfully"). Agent: asked "exposure to the Var wildfire, gross and net, how sure am I?" (`live=true`) → it called `list_events`→`exposure_summary`→`gross_to_net` and returned the governed numbers verbatim (59 props / €164.3m; net €25.0m; ceded €62.2m) and flagged the footprint as SYNTHETIC/frozen for the confidence question — no invented figures. Genie (in-app): "properties in Italy and Austria" → generated SQL over `mv_property`, returned IT 1170 / AT 550. Governance: `/api/governance` returns provenance/history/audit (6/6/6).
- **D33 — Governance / version-transparency views + panel.** `notebooks/07_governance.py` + `gov_data_provenance` (per event: feed source, `is_live`, ingest time, segments, countries), `gov_exposure_history` (threatened count + SI per snapshot — what moved on the book across sweeps), `gov_alert_audit` (append-only dispatch record). App "Governance & provenance" tab renders all three. Job `exposure_07_governance` for reproducibility.
- **D32 — Genie sits on plain-named `mv_*` views, not the numbered tables.** Genie's generated SQL breaks on identifiers starting with a digit (`3_property`), so `mv_property / mv_event / mv_treaty / mv_treaty_layer / mv_coverholder / mv_property_coverholder` (plain names) are the Genie data sources. **Gotcha:** the view *ownership chain* is NOT enough — Genie runs its SQL as the **app SP**, which needs **direct SELECT on the base numbered tables** the views read (granting only the `mv_` views → `MessageStatus.FAILED`). Fixed by granting the SP SELECT on the base tables too.
- **D31 — Genie space created programmatically.** `scripts/create_genie_space.py` (GenieSpaceBuilder + `databricks api post /api/2.0/genie/spaces`) → "Ask the Exposure Book — Bricksurance SE", space `01f1b2bf6cce17eb8815107a4fab826d`, over the `mv_*` views + `gov_data_provenance` + `ref_*`. App SP granted `CAN_RUN`. In-app via the Conversation API (`start_conversation_and_wait` → `get_message_attachment_query_result`), returning Genie's narrative + the SQL it wrote + result rows (trust: you see the query behind the number).
- **D30 — Event Response agent = Claude (FMAPI) with a governed-function tool surface, in-app.** Rather than a served ChatAgent endpoint (heavier to stand up), `app/server/agent.py` runs the tool-calling loop directly against `databricks-claude-sonnet-4-6` (FM API, OpenAI chat schema): 5 tools each wrapping an `fn_*` (`list_events`, `exposure_summary`, `gross_to_net`, `exposure_by_coverholder`, `event_delta`). The model NARRATES; the governed functions DECIDE — every number is a real tool call executed on the warehouse, returned as proof under each answer; the system prompt forbids inventing figures. Served-endpoint packaging is the productionisation step (noted). Response **cache** (`cache_agent_responses`, sha256 key, MERGE upsert) wraps the LLM turn only; the visible **yellow live/cached toggle** switches modes.
